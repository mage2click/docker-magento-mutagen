#!/bin/bash
set -e

[[ -z "$1" ]] && echo "Please specify the version to download (ex. 1.9.4.1)" && exit
mkdir -p src && curl -L http://pubfiles.nexcess.net/magento/ce-packages/magento-$1.tar.gz | tar xzf - -o -C src && \
    (shopt -s dotglob; mv src/magento/* src) && rm -rf ./src/magento/
